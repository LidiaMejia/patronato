import React from 'react';
import './header.css';

export default ({title})=>{
    return(
        <section>
            <header>
                <h1>{title}</h1>
            </header>
        </section>
    )
}